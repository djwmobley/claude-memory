**claude-memory is a session seam — a relay baton handed off between sessions, not a court stenographer.**

It persists durable session state as queryable Postgres rows at the session boundary, serves the minimum context needed to resume effectively, and makes dormant topics retrievable on demand. What it is not: a verbatim transcript, a running log, or always-on surveillance of every utterance.

---

## Documentation index

This index covers documentation that ships in the repository. Local-only planning documents that are excluded via `.gitignore` are intentionally omitted — their absence is deliberate, not an oversight to be corrected.

| Path | Purpose | Status |
|---|---|---|
| `README.md` | Project overview, install, quickstart pointer | updated (added parameter column to command table) |
| `CLAUDE.md` | Project-level operating instructions for Claude Code | current |
| `MANIFEST.md` | This file — tracked index of all documentation | updated (agent-interop doc row added) |
| `QUICKSTART.md` | Step-by-step setup guide for new users | updated (cm#185: corrected the expected `/handoff:init` output — real schema-unit-resolved/applied/verified lines, no phantom phase2-schema.sql/phase3b-schema.sql; loader-stop hook wiring example moved from `Stop` to `SessionEnd`) |
| `PREREQS.md` | System prerequisites (Node, Postgres, pgvector, vLLM) | current |
| `CHANGELOG.md` | Version history and release notes | updated (Unreleased: init-embeddability categorical fix added — pgvector/default-provider total classification at init, assertions write-time embed, backfill-embeddings subcommand, embedding_readiness status/resume reporting; §18.3 feature_usage table + migrate-12-feature-usage.js backfill + usage_query granularity="feature" added; §6.1(b) decisions data migration added; §5.3 absorbed-seam tables + §5.9 fat-card view + §7 write/render/lint libraries added; agent-exchange schema + tamper-evidence infrastructure + AgentProvider/EmbeddingProvider interop contracts added; usage-telemetry record/query library + verify-18 smoke test added, plus the shared smoke-harness extraction; least-cost routing resolver + verify-17 smoke test; schema-setup-only addenda migration; serve-time open_thread staleness gate; agent-interaction guard hooks removed (moved to public judge repo); prior refactor entries retained) |
| `CONTRIBUTING.md` | Contribution guidelines and workflow | updated (removed Step 10 PR-independence hook-test step; guard moved to public judge repo) |
| `CODE_OF_CONDUCT.md` | Community standards | current |
| `SECURITY.md` | Security policy and disclosure process | current |
| `docs/agent-interop.md` | Multi-agent interop: the Postgres shared-access contract, the append-only `agent_exchange` A2A log + compound-cursor watermark polling, `audit_log`/`log_guarded_change()` tamper-evidence semantics, the `AgentProvider`/`EmbeddingProvider` abstract contracts (one-identity-three-surfaces rule), and a worked cross-agent example | added |
| `docs/mcp-tools.md` | The full 35-tool `handoff-mcp.mjs` MCP surface: the two transport shapes (child-process vs. in-process pg), the one project-identity path, `memory_search`'s closed table enum and hybrid scoring, `memory_upsert`/`memory_get`, `memory_lint`, `memory_view_set`/`run`, entity/assertion/edge CRUD (near-match surfacing, revival, supersede), `exchange_append`/`exchange_read`'s compound watermark, `route_resolve`/`routing_profile_set`/`get`, `usage_record`/`usage_query`, and the `persist_decisions` repoint | added + cm#230: `persist_decisions` section notes the SECOND entry point (`payload.decisions[]` on close/checkpoint) now sharing the same write path + §18.3: `usage_query` section documents the new `granularity` parameter (`turn` default / `feature`, the latter reading the new `feature_usage` table, own `groupBy` total classification, `sessionId`+`feature` hard error) + init-embeddability: `assertions.embedding` now has a live write-time path via `writeAssertionWithSupersession`/`embedForWrite` (same fail-soft/counted-`embed_warnings` contract as `decisions` — never a per-row DIVERGENCE line), plus a pointer to the new `backfill-embeddings` subcommand |
| `docs/how-memory-works.md` | Conceptual explanation of the system, session seam model, identity + limitations | updated (staleness refresh: added /handoff:resurrect, de-jargoned north-star-inversion phrasing, removed duplicated north-star tenets, pointer to case-study.md) + coherence pass + param-docs pass + reconcile pass + open_thread staleness gate section (volatile predicates list updated; annotateOnly explanation added) + project-isolation/shared-DB limitation section added (logical-not-physical blast radius; project_id is the only isolation grain; shared-Postgres cross-developer visibility; mitigation levers) + "Schema files" section added (cm#185: the 5-file scripts/sql/ classification table) + Stop hook -> SessionEnd hook terminology fix (implicit close now fires once at true session end, not every turn) + init-embeddability rewrite: pgvector-gated-columns section replaced with the BLOCK-by-default total classification (extension probe before the confirm gate, `--no-embeddings`/`--allow-remote-embed`/`--clear-opt-out`, user-scope `~/.claude/handoff-embed.json` precedence step), plus a new "Backfilling NULL embeddings" section documenting `backfill-embeddings` |
| `docs/glossary.md` | Term definitions for all concepts used across the docs | updated (branch_exists, commit_merged, in_file, pr_state, reality_check, serve-time reality re-probe, [STALE:] annotation entries) + open_thread staleness gate pass (annotateOnly entry added; open_thread entry expanded with staleness gate explanation; [STALE:] annotation, serve-time reality re-probe entries updated to mention open_thread) + cm#233 (open_thread entry updated to mention intentKey; new "resolved_threads (auto-retire)" entry documenting the MATCHED-AND-RESOLVED/UNMATCHED-REPORTED/INVALID-REJECTED/DUPLICATE-COLLAPSED classification) |
| `docs/case-study.md` | Narrative walkthrough of two design problems: Ch.1 decay/operator-pin/resurrection; Ch.2 north-star TDD harness, RED-by-design test methodology, serve-time staleness, adversarial permutation harness, caveman dogfooding | updated (Chapter 2 added) |
| `docs/troubleshooting.md` | Diagnosis guide for common setup and runtime problems | updated (loader-stop hook wiring example moved from `Stop` to `SessionEnd`) |
| `docs/deep/retrieval-contract-evolution.md` | How the retrieval contract schema has evolved over time | current |
| `docs/deep/findings/debate-2026-05-16-memory-bootstrap-collision.md` | Design finding: memory bootstrap collision analysis | current |
| `docs/deep/specs/2026-05-14-bundle-a-substrate.md` | Design spec: Bundle A substrate decisions | updated (cm#185: corrected the stale schema-split table — app-retrieval-events-schema.sql does not require pgvector and is now applied automatically by /handoff:init) |
| `docs/deep/specs/2026-05-16-assertion-extraction-architecture.md` | Design spec: assertion extraction pipeline architecture | current |
| `docs/deep/specs/2026-05-16-memory-bootstrap-collision.md` | Design spec: memory bootstrap collision resolution | current |
| `docs/deep/specs/2026-05-17-predicate-normalization.md` | Design spec: predicate normalization rules | current |
| `docs/deep/studies/2026-05-memory-systems-comparison.md` | Comparative analysis: claude-memory vs. mem0, Graphiti/Zep, Letta, others | current |
| `docs/deep/studies/decay-vs-dont-forget-and-resurrection.md` | Study: decay, devalue-vs-invalidate, and the resurrect ring design | current |
| `commands/handoff/README.md` | Overview of all `/handoff:*` slash commands | updated (added /handoff:resurrect; Stop hook -> SessionEnd hook terminology fix) |
| `commands/handoff/init.md` | `/handoff:init` — first-run provisioning command | updated (removed phantom $2 description positional; added Arguments table + invocation examples for <name> and -y) |
| `commands/handoff/status.md` | `/handoff:status` — read-only project memory status | updated (added --json, --breakdown, --stale-pointers flags with Arguments table + examples) |
| `commands/handoff/close.md` | `/handoff:close` — end-of-session extraction and session-intent persistence | updated (--dry-run flag added: Arguments table + expected output + what is/is not rehearsed; --json alone still works) + reconcile pass (close-time reality reconciliation note added) + cm#230: new "Decisions" section (§4) documenting `payload.decisions[]` persistence, DIVERGENCE surfacing, and fail-soft embedding; subsequent sections renumbered §5-§8 + cm#233: new "Resolved threads" section (§8) documenting the four-way classification; "CLAUDE.md promotion" renumbered §9; dry-run example updated with UNMATCHED/DUPLICATE-COLLAPSED sample lines |
| `commands/handoff/resume.md` | `/handoff:resume` — explicit context load; surfaces `### Session intent` section | updated (serve-time reality re-probe description added; added "Arguments: none" section) |
| `commands/handoff/checkpoint.md` | `/handoff:checkpoint` — mid-session save without ending the session | updated (--note flag added: Arguments table + expected output; --json alone still works) + cm#230: new "Decisions" extraction-instructions item (§5) + example payload field; subsequent items renumbered §6-§7 |
| `commands/handoff/promote.md` | `/handoff:promote` — promote an assertion to CLAUDE.md durable facts | updated (--regenerate [--dry-run] added: rewrite the promotion file from its template without init --force-promotion's ~9 DB/FS writes; Arguments table + expected-output examples + `--regenerate details` section + exit codes) |
| `commands/handoff/drop.md` | `/handoff:drop` — archive prior session memory and start fresh | updated (added "Arguments: none" section) |
| `commands/handoff/purge.md` | `/handoff:purge` — hard delete all project memory (confirmation required) | updated (added --dry-run flag with Arguments table + expected output) |
| `commands/handoff/resurrect.md` | `/handoff:resurrect` — pull a decayed topic back into active context | updated (added --json flag to Flags table + JSON output example + invocation examples) |
| `hooks/README.md` | SessionStart hook setup and behavior; memory-contract-only scope | updated (agent-dispatch guard hook sections removed — those hooks now ship from the public judge repo) |
| `scripts/lib/test-pg-helpers.js` | Shared PG test-harness helpers (pgConnect, createDb/createTestDb, dropDb/dropTestDb, setSetting, getSettingsLike, setContract, makeEnv, runHandoff, runClose, applySchema, resolveProjectId, resolveHandoffMdPath, cleanupHandoffMd, setupProject) extracted from six test scripts | added |
| `scripts/lib/test-env-isolation.js` | Exports `withIsolatedHandoffBaseDir(fn)` — isolates in-process calls into embedding-provider.js's endpoint resolution from a developer machine's real `~/.claude/handoff-embed.json` by pointing `process.env.HANDOFF_BASE_DIR` at a fresh temp dir for the call's duration, then restoring it; shared by scripts/test-sqlite-seam.js and test/test-embed-endpoint-classify.js so "NONE (unconfigured)" assertions can't regress the PR #267 bug locally | added |
| `scripts/lib/predicate-audit.js` | Exports `findUnregisteredPredicates` (pure) and `auditAssertionPredicates` (DB query) — detects predicates used in the assertions table that are not in the declared registry vocabulary | added |
| `scripts/audit-predicates.js` | Ops CLI: connects to configured DB, runs `auditAssertionPredicates` across the live corpus (or a single project via `--project=<uuid>`), exits 0 (all registered), 2 (DB error), or 3 (drift found) | added |
| `test/handoff/test-predicate-vocabulary.js` | CI guard: T1 DB-drift detection (has_updated class), T2 clean corpus passes, T3 strict-mode vocabulary guard, T4 pure helper + locks has_updated/in_file registration | added |
| `test/handoff/test-cmd-params.js` | Focused tests for new command-parameter flags: status --json/--breakdown/--stale-pointers, purge --dry-run, close/checkpoint --json-alone, resurrect --json | added |
| `test/handoff/test-write-path-params.js` | Tests for write-path command parameters: checkpoint --note writes session_note row; promote by content (single/zero/multi match); promote --demote; close --dry-run performs no writes | added |
| `scripts/lib/predicate-registry.json` | Authoritative predicate vocabulary registry | updated (session_note predicate added as 1:N, added_version 1.3) |
| `test/north-star/test-caveman-economy.js` | North-star GREEN gate: dogfoods caveman/telegraphic authoring — proves leaner close payloads reduce bootstrap tokens with zero load-bearing loss (3 arms: economy, fidelity, function-word density) | added |
| `test/north-star/fixtures/caveman-payload.json` | Telegraphic close fixture — identical load-bearing tokens to verbose-payload.json, function words stripped | added |
| `test/north-star/fixtures/verbose-payload.json` | Full-prose close fixture — grammatical sentences, identical load-bearing tokens to caveman-payload.json | added |
| `test/handoff/test-reality-reconcile.js` | Tests for reality-mismatch reconcile (Part 1 + Part 2): R1 branch_exists 1:1 reconcile, R2 idempotency, R3 in_file 1:N reconcile + §7 no-backfill, R4 degraded_close retention prune (keep-most-recent-100 policy) | updated (R4 rewritten for keep-100 policy) |
| `test/handoff/test-degraded-close-retention.js` | Focused unit tests for pruneDegradedClose() in isolation — seeds 110/2/8 records, asserts keep-most-recent-100 invariants, runs on both Postgres and SQLite | added |
| `scripts/test-doc-lint.js` | CI gate enforcing glossary See-also cross-reference resolution, doc-to-real-command validity (/handoff: slash commands + handoff.js engine subcommands), suppression_kind enum sync across SQL schema, cmdPrune validKinds, and the glossary Prune bullet, and MANIFEST.md index integrity (every row in the documentation-index table is exhaustively classified as the header, the separator, or a well-formed backtick-wrapped entry, and every entry's path exists on disk and is not gitignored) | updated (D4c rewritten from an extension allow-list heuristic to a total invariant: every row in the table must be the header, the separator, or a well-formed entry, or the gate fails — no cell shape can silently escape validation) |
| `scripts/test-both-backends.js` | Dual-backend (Postgres + SQLite) integration test harness — S1–S20 covering schema, prune, seam, project-identity, migration, suppression, close-auto-retire, and both-backend parity | updated (cm#185: S12 mirror functions deleted, S12.c/e rewritten to require() and exercise the real applyAdditiveSchema/ensureSchemaCurrent exports; S12.g added — classification cleanliness, excluded-set reason presence, apply-for-both illegality, SQLite retrieval_events.id rowid-alias acceptance test) |
| `scripts/test-payload-schema.js` | Unit tests for payload-schema.js — T1–T26 covering buildPayloadSchema structure, validatePayload, and validateResolvedThreads (T18–T26) | updated (T18–T26 added: resolved_threads schema shape, absent-ok, valid-array, non-array rejection, length-cap, item-type, item-length, boundary acceptance) |
| `scripts/lib/payload-schema.js` | Payload validation helpers — buildPayloadSchema, validatePayload, validateResolvedThreads | updated (resolved_threads added to schema; validateResolvedThreads exported for structural validation of the resolved_threads field) + cm#230: decisions[] item schema documents decision/reason/session_num (topic alone was documented pre-cm#230, before this array was ever actually written) |
| `scripts/lib/reality-checks.js` | L3 Generalized Reality-Binding Registry — REALITY_CHECKS array + runVerifyDispatch + probe implementations | updated (open_thread entry added with annotateOnly:true; getMergedPrSet and probeOpenThread added + exported; annotateOnly flag documented in header and registry entry contract) |
| `scripts/handoff.js` | Core handoff engine — all subcommands | updated (cm#185: generalized applyAdditiveSchema/ensureSchemaCurrent/_computeSchemaFingerprint from a hardcoded two-file set to a total classification of scripts/sql/*.sql; epoch-prefixed EOL/BOM-normalized fingerprint; per-file fail-fast transactions; session-scoped advisory lock; post-apply catalog verification gates the fingerprint upsert; schema_apply_degraded surfaced by status/resume; cmdInit rewired onto the same engine so app-retrieval-events-schema.sql is now applied on fresh init too; loader-stop re-wired from the Stop hook to SessionEnd with an S1 stdin total-classification gate — implicit close now fires once at true session end instead of every turn end; session_in_progress marker is now per-session (JSON array of {session_id, ts} under one row, legacy bare-string markers still honored); loader-hook's SessionStart runs a late-close sweep for stale sibling markers (skipped on source=clear/compact); writeHandoffMd writes atomically via tmp+rename; cmdStatus's session_in_progress display now parses the per-session marker format via getSessionMarkers/latestSessionMarker (legacy bare-string markers still render byte-identically); every marker read-modify-write site is wrapped in withSessionMarkerLock, a transaction-scoped advisory lock via the new db.acquireNamedXactLock port method — no db.dialect branch in the engine itself, S8 abstraction invariant preserved) |
| `scripts/lib/schema-classify.js` | Total classification of scripts/sql/*.sql: non-recursive enumeration, git-tracked cross-check, in-file header-directive vs. schema-manifest.json cross-check, required-roster absence detection | added (cm#185) |
| `scripts/sql/schema-manifest.json` | Tracked classification + expected-objects manifest for every scripts/sql/*.sql file (classification and expected-objects metadata only — never enumeration) | added (cm#185) |
| `scripts/test-schema-bring-forward.js` | Aged-DB fixture + atomic-pair regression proof + fingerprint-stability tests that fresh-DB CI structurally cannot exercise (core-only provision + dropped integrity index + live-duplicate pair; a previously-working index surviving a failed re-create attempt; run-twice idempotency; CRLF/BOM fingerprint stability) | added (cm#185) |
| `.gitattributes` | Forces LF line endings on tracked `*.sql` files regardless of the checkout platform's core.autocrlf | added (cm#185) |
| `scripts/install.js` | Copies `/handoff:*` command files and wires SessionStart/SessionEnd hooks into a Claude Code settings file (user or project scope) | updated (PR #242 follow-up, cm#installjs-hooks-schema: `mergeHooks()` rewritten for Claude Code's real matcher-wrapped `hooks.<Event>: [{matcher?, hooks:[{type:'command',command,...}]}]` schema — the old flat-array detection was invisible to it and appended duplicate entries forever; anchored S2 identity match replaces `.includes()` substring matching; `--hooks-scope user\|project\|auto` targets `~/.claude/settings.json` as well as project `.claude/settings.local.json`; `--dry-run` now prints a unified diff; refuses (exit 2) on malformed/non-object `hooks`, a non-array event value, or a worktree engine path (`--engine-path` + `--dry-run` override for testing); real writes get a timestamped backup + atomic tmp-then-rename; JSON indentation/EOL/BOM of untouched content is preserved) |
| `scripts/test-install-sessionend-migration.js` | Regression guard for install.js's `mergeHooks()` against the matcher-wrapped schema: total-classification coverage (upgrade/move/dedupe/re-point/unrecognized-shape), F-1..F-10 adversary findings, plus file-level scope/dry-run-diff/backup/atomic-write/indent-EOL-BOM-preservation/worktree-refusal tests | updated (rewritten for the matcher-wrapped schema; supersedes the flat-schema-only PR #242 suite; codex-host-adapter: standalone-engine-copy setup now also copies `scripts/lib/*.js`; now wired into `.github/workflows/test.yml`) |
| `scripts/test-loader-stop-gate.js` | Regression guard for cmdLoaderStop's S1 total-classification gate: proves a Stop-shaped/malformed/absent/non-SessionEnd stdin payload is a zero-I/O no-op (timing proof against an unreachable Postgres host), with a contrapositive proof that a genuine SessionEnd payload does attempt real work | added |
| `test/handoff/test-loader-stop-sessionend.js` | DB-backed regression guard for the SessionEnd implicit-close/late-close fix: own-session close-and-clear, no-marker no-op, SessionStart's stale-foreign-marker late-close sweep + DIVERGENCE line, fresh-foreign-marker preservation, source=clear skip, writeHandoffMd atomicity (no `.tmp-` residue), and implicit_close=disabled as a full no-op at both hooks | added |
| `test/handoff/test-session-marker-concurrency.js` | Concurrency contract (modeled on test/migrations/test-routing-profile-concurrency.js) for the session_in_progress marker's advisory-lock-guarded read-modify-write: two concurrent `addSessionMarker` calls for different sessions both survive (no lost update); two concurrent calls for the SAME session dedupe to exactly one marker | added |
| `test/handoff/test-open-thread-verify.js` | Tests for open_thread serve-time staleness gate: OT1 replay real bug (merged PR cited), OT1b real repo probe, OT2 PR not in log (no annotation), OT3 no anchor (no annotation), OT4 fail-soft non-git dir, OT5 safety (annotateOnly: no close-time suppression/degraded), OT6a/b/c getMergedPrSet unit tests | added |
| `scripts/lib/cli-args.js` | Total-classification CLI argument validation for every DB/file-mutating handoff.js subcommand (init, drop, checkpoint, close, purge, promote, queue-drain, prune, retire, backfill-embeddings): every argv token must map to a known flag/value/positional or REJECT (exit 2, zero side effects, before any DB connection); `--help`/`-h` prints usage and exits 0 | added |
| `scripts/handoff.js` | `main()` now runs `enforceTotalClassification()` on every write subcommand's argv BEFORE invoking that subcommand's function, guaranteeing rejection precedes any DB connection or file write; `readStdin()` gains an `allowEmpty` option (empty/TTY stdin on `--json` rejects with exit 2 by default: `--json - requires a payload on stdin`); `cmdClose`/`cmdCheckpoint` require `--json` (with a real stdin payload) or an explicit `--allow-empty` opt-in — a bare `close`/`checkpoint`, or an unrecognized flag such as `--help`, can no longer silently fall through to an extraction-empty write (incident 2026-09-07) | updated |
| `test/test-cli-args-fail-closed.js` | Regression guard for the argument fail-closed spec: unknown-flag rejection + no DB connection attempted, `--help`/`-h` for every write subcommand, `close --help` incident-shape regression, `close`/`checkpoint --json -` with empty stdin rejects, bare `close`/`checkpoint` rejects, `--allow-empty` opt-in reaches the DB-connection stage, loader-stop is confirmed outside the gate (SessionEnd implicit-close path unaffected); 32 ACCEPT tests (PR #272 review) proving every real argv shape from scripts/handoff-mcp.mjs, commands/handoff/*.md, and scripts/test-*.js/test/**/*.js is NOT rejected — added after `init <name> -y` (an undeclared positional) was itself a false-positive reject | added, updated |
| `scripts/lib/memory-view.js` | `runVectorQuery` (the vector-kind interpreter memory_view_run/memory_search already used) is now also exported by reference, so cmdLoaderLoad can run the same canonical path instead of forking a second implementation | updated |
| `test/handoff/test-resume-vector-query.js` | DB-backed regression guard for the vector-kind loader fix: seeds an embedded assertion row + a `{kind:'vector'}` contract entry and asserts loader-load produces `vectorCount > 0` with rendered bullets (mock embedder fixture hit); a fixture-miss (embedder error) case asserts the `— vector query unavailable:` fail-soft line and exit 0 | added |
| `scripts/lib/host-target.js` | codex-host-adapter: total classification of the `--host <claude\|codex>` flag shared by install.js and handoff.js's loader-hook/loader-stop — exact case-sensitive match, no trimming, absent defaults to claude, every malformed shape (empty/unknown/bare/conflicting-repeat) refuses | added |
| `scripts/lib/codex-install.js` | codex-host-adapter: Codex CLI host adapter for install.js — `codex` binary discovery (PATH+PATHEXT walk, synchronous `--version` probe), `${CODEX_HOME:-~/.codex}/hooks.json` merge (reuses install.js's mergeHooks/serializeSettings/unifiedDiff/makeBackupPath by requiring install.js back — install.js's own `module.exports` was moved before its `require.main` guard to make that circular require safe) | added; hardened 2026-09-09 against real codex-cli 0.153.4 — `checkHandoffRegistered` rewritten from a whole-token stdout match to a total classification (NOT_INSTALLED/NOT_REGISTERED/REGISTERED/NEEDS_REPAIR/REGISTERED_UNVERIFIED/UNKNOWN) of `codex mcp get <name> --json` output (BOM/ANSI/banner-line-tolerant JSON extraction, entry resolution across bare-object/array/`servers`/`mcp_servers` shapes, engine-path comparison, `--json`-unsupported plain-text fallback); new `ensureHandoffRegistered` orchestrates backup-before-add + `codex mcp add` + post-add re-verify (anything but REGISTERED/REGISTERED_UNVERIFIED after add is a hard failure); new `backupConfigTomlIfPresent` backs up config.toml before every `add` since it rewrites the whole file; the shared `spawnSync` wrapper now also shell-quotes `args[]` elements containing spaces — a real bug found this session where a space in the engine checkout path was silently split into two argv entries under `shell:true`; independent-review fix 2026-09-09: every `codex` spawn now avoids the shell entirely (shell:false, plain argv array) unless the resolved path is a win32 `.cmd`/`.bat`, in which case every argument is quoted UNCONDITIONALLY (never just when it "needs" it — a bare `&`/`|`/etc. with no whitespace was previously passed unquoted and cmd.exe ran it as a second command) and an argument containing `%`/`!`/newline is refused outright since cmd.exe expands those even inside quotes; new `HANDOFF_CODEX_BIN` env override bypasses PATH discovery (and the cmd.exe shell fallback, if it points at a real executable) |
| `docs/hosts/codex.md` | Canonical Codex CLI host reference: prerequisites, install (MCP registration + hooks.json wiring, exact backup filename pattern), trust (directory trust vs. hooks review, per-hook content hash, `codex exec --dangerously-bypass-hook-trust`), SessionEnd's 3s timeout clamp, the registration-state table, AGENTS.md as the durable-facts file, the full environment-variable table, a forward-reference to the companion PR's skills, manual MCP fallback, uninstall/repair, and a verified-vs-unverified table | rewritten 2026-09-09 (docs/codex-onboarding): reorganized around the onboarding brief's required sections; every existing verified statement carried forward, nothing downgraded |
| `docs/hosts/codex-quickstart.md` | Zero-to-first-close walkthrough for a Codex-only user with no Claude Code: clone/install, Postgres + pgvector, point at the DB, install the Codex host wiring, trust hooks, `handoff_init`, `handoff_resume`, do work, `handoff_close`, verify with `handoff_status` | added (docs/codex-onboarding) |
| `docs/hosts/codex-howto.md` | Day-to-day Codex usage: a working day (resume/checkpoint/close), reading the graph (`memory_search`, `entity_read`, `assertion_read`, `edge_read`, `persist_decisions`), what Postgres stores (13-table live schema, embeddings), degraded modes, promoting durable facts to AGENTS.md, and a 10-item "Known real-world defects and quirks" table (symptom/cause/workaround/status) sourced from a live codex-cli 0.153.4 session | added (docs/codex-onboarding) |
| `scripts/test-install-host.js` | codex-host-adapter test suite: `--host` classification matrix (C1-C14), `discoverCodex` unit tests, `CODEX_HOME` validation (K series), end-to-end `install.js --host codex` subprocess runs against a plain non-worktree engine copy (E series), hooks.json merge/idempotency/cross-host-entry-refusal (H series), space-in-path quoting (Q1), and a Claude-path byte-identical-output proof (F1) | added; hardened 2026-09-09 — R1-R4/A-only coverage replaced/extended with Z1-Z17 (deterministic `spawnSyncImpl`-based total-classification coverage of every registration state, including the BOM/ANSI/banner/array/wrapper-map entry shapes and the adversarial "not found, try codex mcp add handoff" plain-fallback case), BK1-BK3 (config.toml backup skip/create/uniqueness), and a stateful codex stub (an `mcp add` durably changes a later `mcp get`) so E1-E9/H1-H3/Q1 exercise the real backup+add+re-verify workflow (new: E5 stale-path repair, E8 pre-existing-config.toml backup, E9 post-add re-verify failure -> exit 1); independent-review fix 2026-09-09: D3-D5 (`HANDOFF_CODEX_BIN` override), QW1-QW11 (pure `classifyAndQuoteWin32`/`needsCmdShell` unit coverage incl. `%`/`!`/newline refusal), and INJ1-INJ5 (real-spawn shell-injection-safety proof via a REAL stub process — an engine path containing `&`, `\|`, `^`, `(y)`, or a space must reach the child as one unmangled argv token with no side-effect file ever created) |
| `scripts/install.js` | codex-host-adapter: `--host <claude\|codex>` flag (via lib/host-target.js); `--host codex` skips the slash-command copy entirely and delegates to a new `mainCodex()` (lib/codex-install.js) for MCP registration + hooks.json wiring instead of the Claude settings.json path, which is otherwise byte-for-byte unchanged; `isOurs`/`OURS_RE` extended with an optional trailing `--host claude\|codex` group (identity now keyed by verb+host, defaulting to claude when the suffix is absent); `scanEntries`/`mergeHooks` gained a `targetHost` parameter — a recognized-but-wrong-host entry (or a structurally malformed inner hook) is flagged `unrecognizedShape` and left untouched, never silently repointed; `mergeHooks` also gained a `matcherFor` option so a freshly-created group can carry a `matcher` (used for Codex's SessionStart `"startup\|resume"`); `module.exports` moved before the `require.main` guard | updated; hardened 2026-09-09 — `mainCodex`'s MCP registration branch rewritten around `codexInstall.ensureHandoffRegistered`'s classify/backup/add/re-verify state machine (skip / would-add / added / add-failed / abort), printing the config.toml backup path or skip reason, a NEEDS_REPAIR old-vs-new path note, and failing with exit 1 (not 2) specifically when a post-add re-verify can't confirm registration |
| `scripts/handoff.js` | codex-host-adapter: `loader-hook`/`loader-stop` accept and total-classify `--host <claude\|codex>` (refuse exit 2 on a malformed value, before any other I/O); `cmdInit`/`cmdClose` resolve the durable-facts promotion path once (`resolvePromotionFilePath`/`promotionFilename`) and thread it through every write site instead of re-deriving it per branch; the two cosmetic hardcoded `"CLAUDE.md"` stderr literals in the Key-paths-heal notes are replaced with the resolved basename | updated; hardened 2026-09-09 — new `resolveSessionIdFromEnv(host)` total-classifies the `CLAUDE_CODE_SESSION_ID`/`CODEX_THREAD_ID` fallback (a real interactive Codex session sets `CODEX_THREAD_ID`, not `CLAUDE_CODE_SESSION_ID`; both set and differing resolves by `--host`, defaulting to `CLAUDE_CODE_SESSION_ID` when host is unknown, with a one-line stderr notice); `resolveHookSessionId` (loader-hook/loader-stop/resume) and `resolveSessionId`/`clearSessionMarkerForClose` (MCP-invoked close/checkpoint, which never receive `--host`) both route through it; both new functions exported for direct unit testing |
| `scripts/lib/handoff-paths.js` | codex-host-adapter: `resolvePromotionFilePath(root, defaultFilename)` gained an optional second parameter (default filename to use when `HANDOFF_PROMOTION_FILE` is unset — the env var still always wins); new `defaultPromotionFilenameForHost(host)` maps `'codex'` -> `AGENTS.md`, everything else -> the historical `CLAUDE.md` default; both changes are additive — every existing single-argument call site is byte-for-byte unchanged | updated |
| `scripts/test-host-agnostic-naming.js` | P14/P14b/P15 added: `HANDOFF_PROMOTION_FILE` unset + host=codex defaults to `AGENTS.md` (Claude default unaffected), env override still wins over the codex default, and a static source-scan regression guard that no hardcoded `"CLAUDE.md"` stderr literal remains in handoff.js | updated |
| `scripts/test-install-engine-path.js` | Standalone-engine-copy test setup now also copies `scripts/lib/*.js` alongside `install.js` — install.js requires `./lib/host-target` (and, on the codex host path, `./lib/codex-install`) as of the codex-host-adapter change, so a copy of install.js alone no longer runs standalone; now wired into `.github/workflows/test.yml` | updated |
| `scripts/test-loader-stop-gate.js` | H1-H6 added: `--host` total-classification proof-of-firing on both `loader-stop` and `loader-hook` (unknown value / bare / empty inline / conflicting repeat all refuse fast, exit 2, before any I/O; a valid `--host codex` is accepted and proceeds to the normal fast no-op path) | updated; hardened 2026-09-09 — CX1/CX2 added: real Codex SessionStart/SessionEnd hook payload shapes (verified fields, incl. `source`/`reason`) are accepted by loader-hook/loader-stop without crashing; SID1-SID9 added: direct unit coverage of `resolveSessionIdFromEnv`/`resolveHookSessionId`'s `CLAUDE_CODE_SESSION_ID`/`CODEX_THREAD_ID` total classification (neither/one/both-equal/both-differing-by-host cases, plus payload-precedence); SID10-SID11 added (independent-review fix 2026-09-09): whitespace-only and padded env values are trimmed/treated-as-absent |
| `scripts/test-os-portability.js` | P2/P3 allowlists extended for the codex-host-adapter: `scripts/lib/codex-install.js` gets its own sanctioned single `shell:true` occurrence (same "must shell out to a .cmd/.bat wrapper on Windows" reasoning as the existing reality-checks.js exception, count-capped identically) and its `PATH`/`PATHEXT` discovery platform branch; `scripts/test-install-host.js`'s cross-platform stub-construction platform branches are sanctioned the same way test-staleness-permutations.js's already were | updated |
| `README.md` | Host-neutral rewrite of the "only model invocation" framing; new "Supported hosts" section/table pointing at docs/hosts/codex.md | updated (codex-host-adapter) |
| `scripts/handoff.js` | cmdLoaderLoad's `vector` query-kind stub ("skipped in loader (Phase 3.6 hook)") replaced: now runs memory-view.js's `runVectorQuery` (delegating to memory-search.js's `memorySearch`), renders hits as `### Vector query (<text>)` bullets bounded by sectionBudget, and increments `vectorCount`/tokensUsed from real hits so the Done line reports the true count; fail-soft on any embedder/provider error (unreachable vLLM, unconfigured/NONE endpoint, empty query text) — renders exactly one `— vector query unavailable: <reason>` line, never throws, never blank | updated |
| `CHANGELOG.md` | Unreleased entry for the codex-host-adapter feature | updated |
| `templates/codex-skills/handoff/SKILL.md` | codex-skills-and-agents-md: host-neutral dispatcher skill (bare invocation -> status + sub-skill list; never closes) | added |
| `scripts/handoff.js` | `promote --regenerate [--dry-run]` added: rewrite the durable-facts promotion file from its template without `init --force-promotion`'s ~9 unrelated DB/FS writes. cmdInit's `writeFreshPromotionFile` closure extracted to module-level `renderFreshPromotionContent()` (cmdInit's own output is byte-identical); new `writePromotionFileAtomic`, `parseDurableFactsSection` (CRLF-tolerant "## Durable facts" section parse for carry-forward), `detectDominantEol`/`convertEol`, `insertCarriedDurableFacts`, and `classifyPromotionRegenerateTarget` (total classification: absent/file/directory/symlink/other) helpers added. Preconditions (marker resolvable, DB reachable) gate before any write; target-state absent writes fresh with no backup, a regular file is backed up then regenerated with any parseable "## Durable facts" fact lines carried forward (an unparseable section warns instead of carrying), directory/symlink/other exits 1 with nothing written | updated |
| `scripts/lib/cli-args.js` | `promote` SPEC gains `--regenerate`/`--dry-run` as known boolean flags (so the argv gate doesn't reject them before cmdPromote's own body runs); combination validation (--regenerate exclusive of every other promote flag/positional) remains cmdPromote's own job, not this module's | updated |
| `test/handoff/test-promote-regenerate.js` | Coverage for `promote --regenerate [--dry-run]`: absent/regular-file/directory target-state classification, "## Durable facts" carry-forward (parseable and unparseable), --regenerate combined with a positional or an unknown flag (exit 2), --dry-run writes nothing/no backup, CRLF preservation, backup filename has no `:` | added |
| `README.md` | Promote command-table row updated: `--regenerate [--dry-run]` form added alongside id/`--subject`/`--demote` | updated |
| `CHANGELOG.md` | Unreleased entry for `promote --regenerate [--dry-run]` | updated |
| `templates/codex-skills/handoff-status/SKILL.md` | codex-skills-and-agents-md: read-only status sub-skill (`mcp__handoff__handoff_status`) | added |
| `templates/codex-skills/handoff-resume/SKILL.md` | codex-skills-and-agents-md: force-load prior-session context sub-skill (`mcp__handoff__handoff_resume`) | added |
| `templates/codex-skills/handoff-checkpoint/SKILL.md` | codex-skills-and-agents-md: mid-session save sub-skill (`mcp__handoff__handoff_checkpoint`, session_id via `CODEX_THREAD_ID`) | added |
| `templates/codex-skills/handoff-close/SKILL.md` | codex-skills-and-agents-md: end-of-session close sub-skill (`mcp__handoff__handoff_close`) — gated on an explicit user request to close, never fired speculatively | added |
| `templates/codex-skills/handoff-query/SKILL.md` | codex-skills-and-agents-md: free-text search sub-skill (`mcp__handoff__memory_search`) | added |
| `templates/codex-skills/handoff-init/SKILL.md` | codex-skills-and-agents-md: first-run provisioning sub-skill (`mcp__handoff__handoff_init`) | added |
| `templates/codex-skills/handoff-promote/SKILL.md` | codex-skills-and-agents-md: durable-facts promotion sub-skill — honestly states no dedicated MCP tool exists yet and falls back to the `promote` CLI subcommand | added |
| `templates/project-agents-md.tpl` | codex-skills-and-agents-md: Codex-host promotion-file template (AGENTS.md) — project-name title, same operating canon, an "MCP tools" hints section (handoff_resume/checkpoint/close/status, memory_search, entity/assertion/edge_read, persist_decisions), same Key paths marker scheme as the Claude template | added |
| `scripts/lib/codex-install.js` | codex-skills-and-agents-md: skill install (A) — `computeSkillBodyHash`/`extractSkillMarkerHash`/`buildSkillFileText` (sha256-of-normalized-body-excluding-marker-line scheme), `resolveCodexSkillsDir` (`HANDOFF_CODEX_SKILLS_DIR`, relative refused), `classifySkillTarget` total classification (write/unchanged/overwrite/skip/blocked/error via fs.stat, never path-string compare), `installSkills` (atomic tmp+rename writes, `--force-skills` backup-then-overwrite), `findStaleSourceCommandSkills` (`source-command-handoff-*`, reported never deleted/followed); `buildMcpAddArgv`/`buildMcpTomlStanza` also carry `HANDOFF_HOST=codex` alongside `HANDOFF_PROMOTION_FILE=AGENTS.md` | updated |
| `scripts/install.js` | codex-skills-and-agents-md: `--force-skills` flag; `mainCodex()` now also installs skills into `HANDOFF_CODEX_SKILLS_DIR`/`~/.agents/skills` after hooks.json, printing per-skill outcomes + a stale-skill warning list + a written/unchanged/skipped/errors summary, exiting 1 on any skill error/block; new `loadCodexSkillTemplates()` | updated |
| `scripts/lib/claude-md-key-paths.js` | codex-skills-and-agents-md: `resolvePromotionHost` (env `HANDOFF_HOST` total classification, else inferred from `HANDOFF_PROMOTION_FILE`'s basename), `looksLikeFindReplaceCopy` (detects the known-bad hand-made `~/.Codex/`/`# Codex-memory` copy), `healKeyPathsSection` gained an explicit `ambiguous` branch for 2+ managed-by markers in one Key Paths section | updated |
| `scripts/handoff.js` | codex-skills-and-agents-md: `cmdInit` step 11 resolves the promotion host via `resolvePromotionHost` (exit 2 on a malformed `HANDOFF_HOST`), renders `templates/project-agents-md.tpl` instead of `templates/project-claude-md.tpl` when the host is codex, warns on a find-and-replace-copy shape, and supports `--force-promotion` (backup + unconditional regenerate) — the Claude path's fresh-write output is unchanged | updated |
| `scripts/handoff-mcp.mjs` | codex-skills-and-agents-md: `memory_search` tool description now states that only tables present in the project database are actually searched and that existence is probed at call time, not assumed from the 15-table enum (no behavior change) | updated |
| `scripts/test-install-host.js` | codex-skills-and-agents-md: S1-S16 (skill hash/marker stability, total classification a-f incl. dispatcher-blocking and --force-skills, dry-run, stale-entry detection, `HANDOFF_CODEX_SKILLS_DIR` resolution, symlink refusal) and P1-P9 (promotion host resolution total classification, find-and-replace-copy detection, ambiguous-multi-marker heal outcome, Claude-template byte-identical sha256 pin, AGENTS.md template sanity); `setupEngineCopy`/Q1's manual engine copy extended to also stage `templates/codex-skills/`; A1/E3 argv fixtures updated for the new `HANDOFF_HOST=codex` env entry | updated |
| `MANIFEST.md` | codex-skills-and-agents-md rows added | updated |
